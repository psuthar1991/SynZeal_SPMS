@model  Synzeal_Inventory.Entity.Product
@{
    ViewBag.Title = "Company";
    Layout = null;
    string sectionName = ViewBag.sectionName;

}
<div class="col-md-12">
    <!-- general form elements -->
    <!-- /.box-header -->
    <div class="box">
        @*<div class="box-header with-border">
                <h3 class="box-title">Add Query</h3>
            </div>*@
        <!-- form start -->
        <form role="form">
            <div class="box-body">
                <div class="form-group">
                    <label for="exampleInputEmail1">CAT No</label>
                    @Model.Sku
                </div>
                <div class="form-group">
                    <label for="exampleInputEmail1">Remark</label>
                    @*<input id="QuotePrice" name="QuotePrice" type="text" value="@Model.QuotePrice" class="form-control"/>*@

                    <textarea id="QuotePrice" name="QuotePrice" rows="7" class="form-control">
@Model.QuotePrice
</textarea>


                    <input type="hidden" id="quotepriceproductId" name="quotepriceproductId" value="@Model.Id" />
                </div>
                <div class="form-group" style="text-align: right">
                    <button type="button" onclick="Quote.addQuotePrice()" class="btn btn-primary">Save</button>
                </div>
            </div>
        </form>
    </div>
</div>

@if (Model.Id != 0)
{
    <text>
        <script type="text/javascript">

            @*$('textarea#QuotePrice').html('@Html.Raw(Model.QuotePrice)');*@

        </script>
    </text>
}